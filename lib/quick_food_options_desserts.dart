import 'package:flutter/material.dart';

class QuickFoodOptionsDesserts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quick Food Options: Desserts'),
      ),
    );
  }
}
