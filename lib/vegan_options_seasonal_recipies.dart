import 'package:flutter/material.dart';

class VeganOptionsSeasonalRecipies extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vegan Options: Seasonal Recipes'),
      ),
    );
  }
}
