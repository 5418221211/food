import 'package:flutter/material.dart';

class QuickFoodOptionsDinner extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quick Food Options: Dinner'),
      ),
    );
  }
}
