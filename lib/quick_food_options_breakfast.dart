import 'package:flutter/material.dart';

class QuickFoodOptionsBreakfast extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Quick Food Options: Breakfast'),
      ),
    );
  }
}
